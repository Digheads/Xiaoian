<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Xiaoian" parent="android:Theme.Material.NoActionBar" />

    <!-- A bare window for an Activity that only shows a Compose dialog. -->
    <style name="Theme.Xiaoian.Transparent" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
